---
name: memory-retrieval
description: "Deprecated alias for the memory-search skill. Superseded during the 3.0 memory curriculum (GP-856). Invoke memory-search instead; this remains only so the /gutt-pro:memory-retrieval command keeps working."
---

# Memory Retrieval (deprecated → use `memory-search`)

This skill has been superseded by **`memory-search`**, which teaches the
shallow-first ladder, summary-first reading, and token discipline for the gutt
knowledge graph (GP-856).

The trigger phrases that used to auto-fire this skill now belong to
`memory-search`, so it no longer activates on its own — it stays only as an
alias for the existing `/gutt-pro:memory-retrieval` command.

**Use `memory-search`** for all memory reads. For multi-hop traversal see
`graph-traversal`; for writing memory see `memory-capture`.
